# ==============================================================================
# .gitattributes — weighlink
# Enforce LF line endings in the repo. Git converts to native on checkout.
# ==============================================================================

* text=auto eol=lf

# Explicitly binary — never touch these
*.png binary
*.jpg binary
*.mp4 binary
*.tar.gz binary
